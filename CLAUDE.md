## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `paulwu655/saml-sp-test-harness`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
